@Library('jenkins-libraries')

pipeline {
    agent any
    stages {
        stage ('Run only if approval exists') {
            steps {
                script {
                    log.info "message"
                }
                echo "The build has been approved!!!"
            }
        }
    }
}